# 0.1.16 검증 결과 — 2026-09-17

- `scripts/Test-Measurement.ps1 -PowerPoint`: 4,512개 검사 통과. 기존 측정·반전·회전·WASD 검사에 n점원 보간, 면적·둘레, 점 이동·곡률 핸들, 실행 취소·다시 실행, 저장 정보 복원을 추가했습니다.
- 실제 PowerPoint의 소유 대화상자에서 다섯 가지 번호 형식의 그룹 사진을 선택하여 측정 사진 복사를 실행했습니다. 원본 그룹·라벨·위치가 보존되고, 복사본에 사진·측정 곡선·측정값만 들어가는지 검사했습니다. 사진 여러 장과 텍스트를 포함하는 그룹 내부 사진, 회전·반전된 사진도 검사했습니다.
- PowerPoint 라벨의 테두리 색, 2.5 pt 굵기, 바탕색, 투명 설정 및 A)·L) 표시를 확인했습니다. n점원의 점과 곡률 정보가 복사본에서 다시 열리는지 검사했습니다.
- 360×340~1900×1000 창 크기와 125/150/200/300% 추가 확대 조건에서 4열 버튼 정렬·글자 맞춤·겹침 방지·저장 버튼 접근·4:3 확대창을 확인했습니다. 긴 상태 안내가 줄바꿈될 때 스크롤 범위를 갱신하도록 수정했습니다.
- `scripts/Test.ps1 -SkipDialogs`: 2,042,658개 검사 통과. 새 55개 라벨 프리셋·5개 다음 버튼, 상자 서식 콜백, 기존 설정 파일 호환성 및 배열 기능을 포함합니다.
- x86/x64 COM 경계 검사, 공식 RibbonX 스키마 검사, 실제 PowerPoint의 기존 사진·번호 동작 31개 검사를 통과했습니다.
- 설치된 DLL 버전은 0.1.16.0이며 빌드 DLL과 SHA256이 일치합니다. 기존 번호 설정 파일의 해시가 업데이트 전후 동일합니다.
- 확인 자료: `build/qa/measurement-point-editing.png`, `measurement-group-copy.png`, `measurement-dialog-large-dpi.png`.

# 0.1.15 검증 결과 — 2026-09-13

- 측정 도구 20개를 4열 × 5행, 편집 버튼 8개를 4열 × 2행으로 맞췄습니다. 두 영역의 버튼 폭과 높이가 같고 각 열의 왼쪽 위치가 일치하는지 검사했습니다.
- 버튼 높이와 여백을 줄이고 아이콘을 화면 배율에 맞춰 표시합니다. 수동 스케일의 세 버튼도 같은 크기를 사용합니다. 긴 기능명은 유지하며 실제 글자·아이콘의 필요 너비를 계산합니다.
- `scripts/Test-Measurement.ps1 -PowerPoint`: 4,135개 검사 통과. 360×340~1900×1000 및 125/150/200/300% 글자·컨트롤 확대 조건에서 4열 배치, 글자 크기, 겹침, 저장 버튼 접근, 단위 입력 및 4:3 확대창을 확인했습니다. 실제 PowerPoint 측정 사진 복사·저장·방향 보존·WASD 검사도 통과했습니다.
- 현재 1680×1050 모니터에서 창의 클라이언트 영역은 1606×932, 왼쪽 사진 영역은 1013×916입니다. 4:3 사진을 약 989×742로 표시하여 사용할 수 있는 너비를 채웁니다. 검증용 스크린샷은 실제 모니터 크기에 맞춰 생성하도록 변경했습니다.
- 기존 배열·리본·번호 기능 2,042,571개 검사 및 32비트·64비트 COM 경계 검사를 통과했습니다.
- 확인 자료: `build/qa/measurement-dialog-large-dpi.png`, `measurement-dialog-wide.png`, `measurement-photo-size.txt`.

# 0.1.14 검증 결과 — 2026-09-13

- 사진은 항상 왼쪽에서 창 전체 높이를 사용하고, 보기·보정·확대창·도구·서식·결과·저장 패널은 오른쪽에 표시합니다. 작은 화면에서도 위아래 배치로 전환되지 않습니다.
- 오른쪽 내용만 스크롤되며, 매우 큰 글자/작은 창에서 저장 버튼도 오른쪽 스크롤 영역으로 이동하여 겹치지 않게 했습니다.
- `scripts/Test-Measurement.ps1 -PowerPoint`: 1,940개 검사 통과. 실제 PowerPoint 사진 복사·측정값 저장, 방향 보존 및 WASD 검사에 더해 사진의 전체 높이 사용, 패널의 오른쪽 배치, 단위 입력과 버튼 접근을 검사했습니다.
- 검사 실행 파일에 PerMonitorV2 DPI 매니페스트를 추가하여 PowerPoint와 같이 물리 화면 좌표를 사용합니다. 360×340~1900×1000 및 125/150/200/300% 글자·컨트롤 확대 조건을 검사했습니다.
- 3650×1860 클라이언트 영역에서 4:3 사진의 표시 크기는 약 2427×1820입니다. 사용자 첨부 화면의 사진 영역 약 1407×1055와 비교하면 가로·세로 약 1.73배, 면적 약 2.98배입니다. 사진 비율과 화면 설정에 따라 실제 증가량은 달라집니다.
- 기존 배열·리본·번호 기능 2,042,571개 검사와 x86/x64 COM 및 공식 RibbonX 검사를 통과했습니다. 기존 사진·번호 동작 31개 검사도 통과했습니다.
- 확인 자료: `build/qa/measurement-dialog-large-dpi.png`, `measurement-photo-size.txt`, `measurement-dialog-wide.png`.

## 0.1.13 이전 검증 기록

- `scripts/Test-Measurement.ps1 -PowerPoint`: 1,173개 검사 통과.
- 사진과 측정 결과만 같은 슬라이드에 복사하고, 원래 사진·번호 라벨·다른 텍스트를 유지합니다. 여러 사진과 텍스트가 들어 있는 그룹에서 측정 사진 한 장만 복사하는 검사도 통과했습니다.
- 저장·재열기·초기화·반복 측정에서 스케일과 번호 라벨을 유지합니다. 실제 PowerPoint 자유형 도형의 첫 좌표가 미리보기 측정 좌표와 일치하는지 검사했습니다.
- 비대칭 4색 사진의 일반·좌우 반전·상하 반전·동시 반전·90°·180°·27° 회전 및 회전/반전된 그룹을 사용했습니다. 실제 슬라이드 픽셀과 미리보기 픽셀을 비교하고, 다시 저장해도 반전이 중복 적용되지 않음을 확인했습니다.
- WASD의 실제 키 이벤트로 불러온 이미지 기준 1 px 이동, Shift 10 px, 확대/회전 좌표, 실제 Windows 포인터 위치, 클릭 시 미세 좌표 보존, Enter로 스케일 기준 확정을 검사했습니다.
- 고정 창이 모니터 작업 영역의 96%를 채우며, 열릴 때 사진 영역에 키보드 포커스가 있는지 확인했습니다. 360×340부터 1900×1000까지 및 125/150/200/300% 확대 조건에서 겹침, 단위 입력 잘림, 저장 버튼 접근과 확대창 4:3 비율을 검사했습니다.
- `scripts/Test.ps1 -SkipDialogs`: 기존 배열·리본·번호 기능 2,042,571개 검사 통과. 치수측정 버튼이 Lab Photo Tools 탭 안에 포함되는지 확인했습니다.
- x86/x64 COM 경계 검사와 공식 RibbonX 스키마 검사 통과. 실제 PowerPoint의 기존 사진·번호 동작 31개 검사 통과.
- 검증에 사용한 합성 사진과 테스트 프레젠테이션은 `build/qa`에 있습니다. 사용자 프레젠테이션은 저장하지 않았습니다.

## 0.1.12 이전 검증 기록

## 치수측정 검증

- `scripts/Test-Measurement.ps1 -PowerPoint`: 1,883개 검사 통과.
- 선·평행선·3점원의 보정식, nm/µm/mm 단위 환산, 선·원·사각형·타원·각도·다각형·이차 곡선 계산, 퇴화 도형 거부, 확대 좌표 불변성, 실행 취소/다시 실행, CSV 및 문서 직렬화를 확인했습니다.
- 실제 PowerPoint에서 20개 수동 도구의 도형/텍스트 생성, 원본 슬라이드 유지, 사진 그룹화, 저장 후 재열기, 측정 사진의 번호 라벨 추가 및 자동 배열, 반복 적용 시 중복 방지, 초기화 시 번호 그룹 보존을 확인했습니다.
- 자르기·뒤집기·회전이 적용된 번호 사진 그룹의 측정 및 비례 크기 변경 후 스케일 유지 검사를 통과했습니다.
- 대화상자 360×340, 780×520, 1024×680, 1280×830 크기와 125%, 150%, 200%, 300% 글자/컨트롤 확대 조건을 검사했습니다. 배율 확대 테스트에서는 780×520, 1340×700, 1900×1000을 사용했습니다. 작은 화면은 스크롤로 조작할 수 있고, 적용 버튼도 스크롤하여 완전히 볼 수 있음을 검사했습니다.
- `scripts/Test.ps1 -SkipDialogs`: 기존 배열·리본·번호 서식 2,042,572개 검사 통과.
- `tests/QuickPowerPointValidation.cs`: 실제 PowerPoint 기존 사진·번호 동작 31개 검사 통과.
- `scripts/Test-ComBoundary.ps1`: 32비트·64비트 COM 검사 통과. 공식 `customui14.xsd`로 리본 XML 검사 통과.
- 미리보기와 실제 슬라이드를 PNG로 출력하여 큰 창·작은 창 및 회전 사진의 측정 결과를 직접 확인했습니다.

사진은 검증용 합성 이미지입니다. 서로 다른 실제 현미경/카메라의 광학 왜곡·물리적 정확도나 다른 PC의 Office를 검증한 것은 아닙니다. 스케일과 측정 위치는 사용자가 대화창에서 지정하는 값이며, PowerPoint에서 직접 수정한 측정 도형은 저장된 계산 정보에 역으로 반영하지 않습니다.

검증 산출물: `build/qa/measurement-dialog-wide.png`, `measurement-dialog-small.png`, `measurement-slide.png`, `measurement-rotated-cropped-slide.png`, `measurement-validation.pptx`.

## 0.1.11 이전 검증 기록

## 이번 변경

- 빠른 번호 매기기는 사진의 저장 태그만 보지 않고 현재 사진·라벨 그룹의 실제 구성도 확인합니다.
- 사용자가 그룹 안의 라벨만 삭제하면 남은 사진을 다시 라벨 없는 사진으로 인식하고 새 라벨을 붙여 그룹화합니다.
- 모든 사진에 라벨이 붙어 있거나 사진이 없는 경우에는 독립 라벨을 빈 위치에 추가합니다.
- 알아서 배열과 선택 사진의 자동 배열·간격 조절은 사진·라벨 그룹을 함께 이동하고 크기를 조절합니다.

## 실행 결과

- `scripts/Test.ps1 -SkipDialogs`: 2,042,565개 단언 통과. 사진 배열 규칙, 왼쪽 정렬, 20% 여백, 리본 XML, 알파벳 라벨, 글꼴·크기·색과 17 pt/34 pt 비례식을 검사했습니다.
- 공식 RibbonX `customui14.xsd` 검증 통과.
- `scripts/Test-ComBoundary.ps1`: 32비트와 64비트에서 COM 수명 주기와 리본 XML 호출을 모두 통과했습니다.
- `tests/QuickPowerPointValidation.cs`: 실제 PowerPoint에서 31개 단언 통과. 사진 라벨 삭제 후 같은 사진의 재인식·재그룹화, 라벨 사진의 알아서 배열, 모든 사진이 라벨링된 슬라이드와 사진 없는 슬라이드의 독립 라벨, 저장 후 다시 열었을 때의 번호 연속성을 확인했습니다.
- 검증용 PowerPoint는 저장 여부를 묻지 않고 닫았고, 최종 설치 전 PowerPoint 프로세스가 종료된 것을 확인했습니다.

## 확인 자료

`build/qa`에는 `ribbon.xml`, `smart-layout.png`, `number-labels.png`, `number-format-0.png`부터 `number-format-2.png`, `LabPhotoTools-0.1.11-quick-validation.pptx`가 있습니다. 이 산출물은 테스트에서 만든 파일입니다.

## 검증 범위

다른 PC의 실제 32비트 Office, 모든 디스플레이 드라이버와 모니터 간 DPI 전환은 실기 검증하지 않았습니다. 창 폭이 좁으면 Office가 리본 그룹을 접으며, 창을 최대화하면 더 많은 컨트롤이 표시됩니다.
